% Find the edge of symbol
function [ffo] = rx_estimate_ffo(rx_signal, sim_options)

if strfind(sim_options.FreqSyncMode, 'autocorrelation')
    % autocorrelation
end

if strfind(sim_options.FreqSyncMode, 'replica')
    
    
end
